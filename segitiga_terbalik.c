#include stdio.h

int main()
{
    int x,y,n;
    printf(masukkan jumlah n = );
    scanf(%d,&n);
    for(x=1; x=n; x++)
    {
        for(y=1; y=x; y++)
        {
            printf( );
        }
        for(y =1; y =((n2)-((2x)-1)); y++)
        {
            printf(%d, x);
        }
        printf(n);
    }
    return 0;
}